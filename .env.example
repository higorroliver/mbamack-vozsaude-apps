# Vozes da Saúde - Variáveis de Ambiente

# Nível de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Caminho para o ChromeDriver (opcional - será detectado automaticamente)
# CHROME_DRIVER_PATH=/usr/local/bin/chromedriver
